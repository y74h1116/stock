- Lesson 4 メモ
    ```
    $ mkdir simple_server
    $ cd simple_server
        # 初期化(package.jsonの生成)
        # エントリポイントに main.js を指定する
    $ npm init
        # http-status-codes パッケージをアプリケーションを依存ファイルとして保存
    $ npm i http-status-codes -S
    ```
